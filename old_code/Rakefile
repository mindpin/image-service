APP_FILE  = 'lib/app.rb'
APP_CLASS = 'ImageServiceApp'

require 'sinatra/assetpack/rake'
require 'sinatra/assetpack/rake'
require "./lib/app"

Dir["./lib/tasks/*.rake"].each {|task| import task}
