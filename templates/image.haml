.image-show
  %h1
    %i.icon.ion-image
    = @image.original

  - if @image.file_processing || @image.file_tmp

    .image
      %h4{style:"color:#ccc"}= "图片处理中, 请稍后刷新页面"

  - else
  
    .image
      %h4 原始图片
    
      %a.url.icon.ion-link{href: "/display?url=#{@image.file.url}"}
  
      %input.copy{type: "text", value: @image.file.url, readonly: true}

    - @image.versions.each do |version|
      - name  = version[:name]
      - value = version[:value]
      - url   = version[:url]
      - cn    = OutputSetting.names[version[:name]]
    
      .image
        %h4= cn
      
        %span
          - if value.size == 2
            #{value[0]} px
            %span.icon.ion-ios7-close-empty
            #{value[1]} px
          - else
            #{value[0]} px
      
        %a.url.icon.ion-link{href: "/display?url=#{url}"}
  
        %input.copy{type: "text", value: url, readonly: true}
